dependencies:
  pre:
    - bash ./scripts/install-nix.sh
  cache_directories:
    - "~/nix-1.10-x86_64-linux"

test:
  override:
    - make test

deployment:
  testing:
    branch: deployment
    commands:
      - bash ./scripts/deploy.sh $(readlink result)
