dependencies:
  cache_directories:
    - ~/nix
  pre:
    - bash ./install-nix.sh

test:
  override:
    - make test

deployment:
  testing:
    branch: deployment
    commands:
      - nix-copy-closure --to $DEPLOY_USER@$DEPLOY_HOST $(readlink result)
