dependencies:
  pre:
    - bash ./scripts/install-nix.sh

test:
  override:
    - make test

deployment:
  testing:
    branch: deployment
    commands:
      - bash ./scripts/deploy.sh $(readlink result)
